# חוב פתוח — דף הנחיתה של InPlace

**נכתב:** 27.08.2026, בסוף שלבים 1 עד 3 של עבודת ה‑SEO (ענף `seo/pre-launch`).

הקובץ הזה מרכז את מה ש**לא** נסגר, ולמה. הוא לא רשימת רעיונות: כל שורה כאן היא דבר
שהיה בתוכנית ונשאר פתוח, עם הסיבה ועם מה שסוגר אותו. מה שכן נסגר מתועד ב‑[GATES.md](GATES.md)
עם המדידה שלו.

שלושה סוגים, והם לא שווים במשקל:

- **חוסם השקה** — הדף לא יכול לעלות לאוויר לפני זה.
- **ממתין למידע שרק הבעלים מחזיק** — אפשר להתקדם בכל השאר.
- **נדחה בהחלטה** — נבדק, הוחלט לא עכשיו, והסיבה רשומה.

---

## חוסם השקה

### 1. מיגרציית המכסות

הדף מפרסם **20 / 40 / 150 / 375** מסמכים בחודש. הפרודקשן עדיין על **25 / 50 / 200 / 500**.

זה לא באג בדף. `src/content/he.ts` מצטט את קטלוג ההשקה, ו‑`g14-figures` מוודא שהמספר על
המסך זהה לקטלוג בקוד; מאז שלב 2 גם ה‑JSON‑LD נגזר מאותו מקור ו‑`g21-schema` משווה בין
השניים. כל השרשרת עקבית עם עצמה ולא עם השרת.

**מה שסוגר:** מיגרציית המכסות עולה לפרודקשן לפני שהדף עולה. לא להפך.
**מי מחליט:** לא החלטת עיצוב. סדר פריסה.

### 2. התנהגות ה‑404 אצל המארח

`dist/404.html` קיים, עצמאי ונושא `noindex`, ו‑`g17-crawl` מוודא את זה. מה שאי אפשר
לוודא כאן הוא ה**קוד** שמוחזר: זו התנהגות של Cloudflare Pages, והשרת שהשערים מריצים
אינו Cloudflare.

לפני התיקון כל כתובת שגויה החזירה 200 עם דף הבית, וזה בדיוק מה שהופך מספר בלתי מוגבל
של כתובות לדפים חוקיים בעיני גוגל.

**מה שסוגר:** בקשה אחת לכתובת שאינה קיימת על הדומיין החי, ובדיקה שחוזר 404.
**חשוב:** אין להוסיף כלל catch‑all של `/* /index.html 200`. הוא יחזיר את הבעיה במלואה.

---

## ממתין למידע שרק הבעלים מחזיק

### 3. זהות עסקית: שם חברה מלא, כתובת, טלפון

עמוד `/about/` נבנה ומדבר על המוצר, על שלושת התפקידים ועל העקרונות שלפיהם הוא נבנה.
מה שאין בו הוא מי עומד מאחוריו.

זה משפיע בשני מקומות:

- ה‑`Organization` ב‑JSON‑LD יוצא בלי `address` ובלי `telephone`. זו הייתה החלטה
  מפורשת: **ארגון עם כתובת מומצאת גרוע מארגון בלי כתובת**, ומדובר בעמוד שמדבר על כסף
  של אנשים אחרים.
- אמון. „מי עומד מאחורי המוצר” הוא אחד ממדדי האמון המרכזיים שגוגל מודד, והוא הדבר
  הראשון שקונה B2B מחפש לפני שהוא מכניס ספקים למערכת.

**מה שסוגר:** שלושה שדות. שם רשום, כתובת, טלפון.
**מי מחליט:** הבעלים. אף אחד אחר לא מחזיק את המידע הזה.

### 4. אזכורי מותג

לפי מחקר Ahrefs מדצמבר 2025, אזכורי מותג מנבאים ציטוט במנועי AI פי 3 טוב יותר מקישורים
נכנסים. אחרי שלב 2 הדף **קריא** למנועים האלה (9,014 תווים בלי הרצת קוד); מה שאין הוא
סיבה שיזכירו אותו.

**מה שסוגר:** יוטיוב, לינקדאין ופורומים מקצועיים בעברית.
**מי מחליט:** עבודה שיווקית. לא עבודת קוד, ולכן לא נעשתה כאן.

---

## דורש בדיקה חוזרת אחרי ההעלאה

שלושה דברים לא ניתנים למדידה על שרת מקומי, ולכן הם רשומים גם בסוף [GATES.md](GATES.md):

| # | מה | איך בודקים |
|---|---|---|
| 5 | כותרות אבטחה והפניית HTTPS | `public/_headers` מצהיר עליהן; רק Cloudflare יכול לאשר שהוא קרא את הקובץ |
| 6 | `robots.txt` מוגש כטקסט | `curl -I` על הדומיין החי. צריך `text/plain`, לא `text/html` |
| 7 | נתוני מהירות אמיתיים (CrUX) | מדידת שדה, לא מעבדה. חודש אחרי ההשקה |

### 8. הפניית www לדומיין הראשי

נעשית בכלל Redirect Rule בלוח הבקרה של Cloudflare, לא בקובץ בפרויקט: כלל בין דומיינים
לא שייך לפרויקט ה‑Pages. בלעדיו `www.inplace.digital` ו‑`inplace.digital` הם שני אתרים
שמתחרים זה בזה, וה‑`canonical` לבדו מצמצם את הנזק אבל לא מבטל אותו.

### 9. Search Console ואנליטיקס

לא מחוברים. בלעדיהם אין דרך לדעת אילו מהעמודים החדשים נסרקו, מה נכלל באינדקס, ואילו
חיפושים מגיעים בפועל. יש להגיש את ה‑sitemap ביום ההעלאה.

---

## נדחה בהחלטה

### 10. Content‑Security‑Policy

`public/_headers` מצהיר על `X-Content-Type-Options`, `Referrer-Policy`,
`Strict-Transport-Security`, `Permissions-Policy` ו‑`X-Frame-Options`. **CSP לא נכנס.**

הסיבה: motion כותב סגנונות inline על כל אלמנט מונפש. `style-src` הדוק היה עוצר את
האנימציות בלי לעצור את הדף, וזו הצורה הגרועה ביותר של תקלה — כזו שאף אחד לא מבחין בה.

**מה שסוגר:** להיכנס תחילה כ‑`Content-Security-Policy-Report-Only`, ולאכוף רק אחרי
שהדוחות ריקים.

### 11. `preload` בהצהרת ה‑HSTS

`Strict-Transport-Security` נשלח בלי הטוקן `preload`. הוספתו קרובה לבלתי הפיכה והיא
חלה על כל תת‑הדומיינים, כולל `app.inplace.digital`.

**מי מחליט:** הבעלים, במפורש. לא תופעת לוואי של פריסת דף נחיתה.

### 12. זמן חסימה כשער

`g22-startup` מודד את **משקל הקוד** שנטען בהתחלה, ולא את זמן החסימה. זו לא עצלות; זה
מה שנשאר אחרי שלוש שיטות שנכשלו. הפירוט המלא נמצא ב‑[GATES.md](GATES.md) תחת G22, וכולל
את המספרים: אותה בנייה נמדדה בין 40 ל‑722 אלפיות שנייה על מכונה אחת.

**מה שסוגר:** מדידת שדה אמיתית (INP) אחרי ההשקה. במעבדה, על מכונה משותפת, המספר הזה
מודד את המכונה.

---

## חוב שנוצר מעבודה במקביל

### 13. המיזוג עם `-copy` ועם `-i18n`

העבודה הזאת רצה בענף נפרד כי שני סשנים אחרים ערכו את אותם קבצים באותו זמן. בזמן
הכתיבה, הענף השני בונה **לוקאל אנגלי** תחת `/en/`. שלושה דברים ידרשו תשומת לב במיזוג:

- **`vite.config.ts`** — שני הענפים נגעו בו. כאן נוסף `manualChunks`; שם נוספו כניסות
  לעמוד האנגלי.
- **`hreflang`** — `g17-crawl` כבר בודק שכל יעד `hreflang` הוא עמוד שנבנה בפועל. הבדיקה
  עוברת ריקה כאן כי אין תגי `hreflang` בענף הזה; ברגע שהם מגיעים היא מתחילה לעבוד.
- **ה‑sitemap וה‑prerender** — ה‑sitemap נגזר מהעמודים שנבנו, אז `/en/` ייכנס אליו לבד.
  ה‑prerender לעומת זאת מחזיק רשימת עמודים מפורשת (`PAGES` ב‑`scripts/prerender.mjs`),
  ולוקאל שני ידרוש כניסה משלו שם.

### 14. תרגום העמודים התומכים

ששת העמודים ב‑`src/content/pages.ts` קיימים בעברית בלבד. אם הלוקאל האנגלי נכנס, הם
יצטרכו החלטה: לתרגם, או להשאיר את `/en/` כדף אחד. **תרגום מכונה של עמוד שמבטיח יכולות
מוצר הוא בדיוק המקום שבו הבטחה משתנה בלי שאיש שם לב.**

---

## מה שכבר רשום במקום אחר

- **`SplitHeading` ו‑`&nbsp;`** — הרכיב מתעד את עצמו כמי ששומר מילים מחוברות כיחידה
  אחת, ובפועל מפרק אותן. רשום בסוף [COPY‑SUGGESTIONS.md](COPY-SUGGESTIONS.md), ולא
  שובר דבר היום.
- **שש הצעות קופי** שנכתבו כדי שהבעלים יחליט ולא בוצעו. שם, בטבלה האחרונה.
- **ההמלצות בדף** מסומנות `placeholder: true` ומוצהרות בגוף העמוד כדוגמאות ניסוח.
  `g15-placeholders` מוודא שההצהרה לא תיעלם בשקט. יוחלפו בהמלצות אמיתיות אחרי ההשקה,
  ורק אז אפשר יהיה לשקול `Review` בנתונים המובנים.
