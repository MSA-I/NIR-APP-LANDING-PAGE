# DEPLOY — העלאת דף הנחיתה לאוויר

הכתובת היעד היא **`https://inplace.digital`** — הכתובת הראשית של המותג. האפליקציה
כבר יושבת על `app.inplace.digital` ואינה מושפעת משום דבר במסמך הזה.

המסמך הזה הוא runbook: מה נבדק מראש, מה רק הבעלים יכול לעשות, מה מוגדר בדיוק,
ומה נמדד אחרי — עם גלגול אחורה לכל צעד. הוא נכתב 31.08.2026 מול הקומיט `a882be3`.

---

## 1. מה כבר קיים

| | מצב, כפי שנמדד |
|---|---|
| הדומיין `inplace.digital` | רשום ב-NameCheap, מואצל ל-Cloudflare (`clyde` / `rose`), האזור `active` |
| `app.inplace.digital` | חי, `200`, פרויקט Pages בשם `supplyflow` |
| `inplace.digital` (apex) | **אין רשומה כלל** — הכתובת לא עונה |
| `www.inplace.digital` | `NXDOMAIN` |
| הריפו | `MSA-I/NIR-APP-LANDING-PAGE`, ענף `main` |
| פרויקט Pages לדף הנחיתה | **לא קיים** — זה מה שהמסמך הזה יוצר |

ה-apex ו-`www` ריקים **במכוון**: בעת מעבר הדומיין נמחקו רשומות דף-החניה של NameCheap
כדי שהכתובת של המותג לא תגיש דף זר. אין מה לגלגל אחורה שם — אין מה לדרוס.

---

## 2. מה נבדק מראש, ועבר

הבנייה הורצה על **עותק נקי** של `origin/main`, בדיוק כפי ש-Cloudflare מריץ אותה
(`git clone --depth 1` → `npm ci` → `npm run build`):

| # | מה נבדק | תוצאה |
|---|---|---|
| D1 | `npm ci` מהעותק הנקי | `added 91 packages … 0 vulnerabilities`, יצא `0` |
| D2 | `npm run build` מהעותק הנקי | יצא `0`. ‏`tsc --noEmit` נקי, ‏`vite build` ב-6.46s, ‏18 עמודים, sitemap ו-llms.txt |
| D3 | הבנייה אינה דורשת דפדפן | ‏`prerender.mjs` משתמש ב-Vite בלבד. אין Playwright בשרשרת הבנייה — שרת הבנייה של Cloudflare לא יוכל להריץ דפדפן |
| D4 | גודל קובץ בודד מול תקרת Pages | הגדול ביותר `film-en.mp4` = **9.90 MiB**, התקרה **25 MiB** |
| D5 | מספר קבצים מול תקרת Pages | **202**, התקרה **20,000** במסלול החינמי |
| D6 | זמן בנייה מול תקרת Pages | שניות, התקרה **20 דקות** |
| D7 | חוקי `_headers` מול התקרה | **9** חוקים, התקרה **100** |
| D8 | הפלט מכיל את מה שצריך | `404.html`, `_headers`, `robots.txt`, `sitemap.xml`, קובץ מפתח ה-IndexNow — כולם ב-`dist/` |
| D9 | הכתובות בפלט | ‏45 אזכורים של `https://inplace.digital` ו-12 של `https://app.inplace.digital`. אין כתובת זרה |

**תיקון אחד נדרש ובוצע:** נוסף `.node-version` עם `22.16.0`. בלעדיו פרויקט שנוצר על
תמונת הבנייה הישנה של Cloudflare (‏v2) היה מקבל Node 18.17.1, ו-Vite 7 דורש 20.19 ומעלה —
כלומר הבנייה הראשונה הייתה נכשלת. הקובץ מקבע את הגרסה בלי תלות בתמונה שהפרויקט יקבל.

---

## 3. השער האנושי

חיבור ריפו ב-GitHub לפרויקט Pages מחייב אישור OAuth של אפליקציית Cloudflare בחשבון
ה-GitHub. זו פעולת דפדפן שרק הבעלים יכול לבצע — אין לה תחליף ב-API, ואין כאן מפתח
גישה ל-Cloudflare.

### ההגדרות המדויקות

בלוח הבקרה של Cloudflare: **Workers & Pages → Create → Pages → Connect to Git**

| שדה | ערך |
|---|---|
| Repository | `MSA-I/NIR-APP-LANDING-PAGE` |
| Project name | `inplace-landing` |
| Production branch | `main` |
| Framework preset | `None` |
| Build command | `npm run build` |
| Build output directory | `dist` |
| Root directory | `/` (ברירת המחדל) |
| Environment variables | **אין**. הבנייה אינה קוראת אף משתנה |

שם הפרויקט הופך לכתובת `inplace-landing.pages.dev`. **אין להשתמש בשם `supplyflow`** —
זה הפרויקט של האפליקציה.

הבנייה הראשונה תרוץ מיד עם היצירה ותפרסם ל-`inplace-landing.pages.dev` בלבד. **בשלב הזה
הדומיין עדיין לא מצביע לשם, ולכן שום דבר ציבורי לא משתנה** — זה מכוון: קודם מאמתים על
כתובת ה-`pages.dev`, ורק אחר כך מחברים את הדומיין.

---

## 4. סדר הפעולות

הסדר אינו שרירותי. חיבור הדומיין נעשה **אחרי** שהבנייה הוכיחה את עצמה, והרשומה ב-DNS
נוצרת **אחרי** שהדומיין מחובר בפרויקט — זה הסדר שהתיעוד של Cloudflare דורש, וזה גם
הסדר שעבד במעבר של `app.inplace.digital`.

| # | פעולה | מי |
|---|---|---|
| 1 | יצירת פרויקט ה-Pages לפי §3 | **הבעלים** (שער OAuth) |
| 2 | אימות הבנייה הראשונה ו-`inplace-landing.pages.dev` — 18 עמודים, 404 אמיתי, כותרות | הסוכן |
| 3 | חיבור `inplace.digital` כ-custom domain לפרויקט | הבעלים או הסוכן (עם מפתח) |
| 4 | רשומת DNS ל-apex → `inplace-landing.pages.dev` (Cloudflare משטח CNAME ב-apex) | כנ״ל |
| 5 | כלל הפניה `www.inplace.digital` → `https://inplace.digital` (301) | כנ״ל |
| 6 | חבילת האימות החי של §5 | הסוכן |
| 7 | הגשת ה-sitemap ב-Search Console | הבעלים |

**גלגול אחורה בכל שלב:** ניתוק ה-custom domain ומחיקת הרשומה מחזירים את המצב לקדמותו
בדיוק — ה-apex חוזר להיות ריק, וזה מה שהוא היום. האפליקציה על `app.` לא נוגעת בשום
שלב.

---

## 5. מה נמדד אחרי, ורק אז זה "באוויר"

חמישה דברים אי אפשר למדוד על שרת מקומי. הם רשומים גם ב-[DEBT.md](DEBT.md) כסעיפים
5–9, וזו הרשימה שסוגרת אותם:

| # | מה | איך נמדד | סוגר |
|---|---|---|---|
| L1 | הדף עולה | `200` על `https://inplace.digital/`, ו-18 העמודים מה-sitemap עונים `200` | DEBT §20 |
| L2 | 404 אמיתי | בקשה לכתובת שאינה קיימת חייבת להחזיר **`404`**, לא `200` | DEBT §2 |
| L3 | כותרות אבטחה | `curl -I` — חמש הכותרות מ-`public/_headers` חייבות לחזור | DEBT §5 |
| L4 | `robots.txt` כטקסט | `content-type: text/plain`, לא `text/html` | DEBT §6 |
| L5 | הפניית www | `www.inplace.digital` מחזיר `301` ל-apex | DEBT §8 |
| L6 | הפניית HTTPS | `http://` מחזיר הפניה ל-`https://` | DEBT §5 |
| L7 | IndexNow | הריצה האוטומטית אחרי הדחיפה חייבת לדווח שליחה, לא `IndexNow not sent` | — |

**‏`L2` הוא הקריטי.** אסור להוסיף קובץ `_redirects` עם `/* /index.html 200`. הכלל הזה
הופך כל כתובת שגויה ל-`200` עם דף הבית, וזה בדיוק מה שהופך מספר בלתי-מוגבל של כתובות
לעמודים חוקיים בעיני גוגל. הריפו הזה **אינו** SPA — כל 18 העמודים קיימים כקבצים, ו-Cloudflare
מגיש את `404.html` לבד לכל מה שלא נמצא. אין מה להוסיף.

`L7` תלוי בעיתוי: התהליך ב-[.github/workflows/indexnow.yml](.github/workflows/indexnow.yml)
ממתין ארבע דקות אחרי כל דחיפה ל-`main` וקורא את ה-sitemap **החי**. כל עוד הדומיין לא
מחובר הוא יוצא בשקט עם `IndexNow not sent`, וזה תקין. אחרי החיבור אפשר להריץ אותו ידנית
פעם אחת (`workflow_dispatch`) בלי להמציא קומיט.

---

## 6. מה שהבעלים צריך לדעת לפני שזה ציבורי

**הדף מפרסם מספרי מסלולים שהמוצר החי לא מאשר** — ‏[DEBT.md](DEBT.md) §1, נמדד 31.08.2026.
הכיוון הוא לטובת הלקוח: בפרודקשן כל היכולות פתוחות בכל המסלולים, בעוד הדף אומר שבמסלול
בסיס חלקן חסומות. כלומר הדף **מבטיח פחות** ממה שהמוצר נותן. זו אינה חשיפה משפטית, אבל
שתים-עשרה קבוצות מספרים בדף אין להן היום מקור חי. זה נסגר בפריסת המיגרציות `0243`–`0267`
לאפליקציה, לא בשינוי כאן.

**זו החלטה של הבעלים ולא של הפריסה.** אפשר לעלות ככה ולסגור אחר כך, ואפשר לחכות.

---

## 7. מה במפורש לא נעשה כאן

- **אין `_redirects`.** ראה `L2`.
- **אין CSP.** הוחלט ב-`public/_headers`: מגיע בשלב נפרד כ-Report-Only קודם.
- **אין `preload` ב-HSTS.** הוא כמעט בלתי הפיך וחל גם על `app.inplace.digital`, ולכן
  הוא החלטה של הבעלים ולא תופעת לוואי של פריסת דף נחיתה.
- **לא נוגעים ברשומות ה-MX וה-SPF של השורש.** הן מישור הדואר של הדומיין.
- **לא נוגעים בפרויקט `supplyflow`, ב-Supabase ובאפליקציה.** הפריסה הזאת מוסיפה
  פרויקט שני ורשומת DNS אחת, ולא נוגעת בשום דבר שחי היום.
