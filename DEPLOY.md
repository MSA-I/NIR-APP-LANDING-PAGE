# DEPLOY — דף הנחיתה באוויר

**‏`https://inplace.digital` חי מ‑31.08.2026.** המסמך הזה הוא הפנקס: מה נבדק לפני,
מה נעשה בפועל, מה נמדד על השרת החי, ומה נשאר פתוח.

האפליקציה על `app.inplace.digital` לא נגעה בשום שלב, ונמדדה `200` אחרי כל צעד.

---

## 1. איך זה בנוי

| | |
|---|---|
| חשבון Cloudflare | `7787d2e04e755ec655f8506124aa0ecf` |
| האזור | `inplace.digital`, ‏`82a4bdef942f4f4393d17b516fce7b5a`, ‏`active` |
| פרויקט ה‑Pages | **`inplace-landing`**, ענף ייצור `main` |
| כתובת הפרויקט | `inplace-landing.pages.dev` |
| הדומיין | `inplace.digital` — custom domain על הפרויקט, ‏`status=active`, ‏`cert=active` |
| הרשומה ב‑DNS | `CNAME inplace.digital → inplace-landing.pages.dev`, proxied |
| `www` | `CNAME www → inplace.digital` proxied, ו‑Single Redirect שמפנה אותו ב‑301 |
| שיטת הפריסה | העלאה ישירה, אוטומטית מ‑GitHub Actions בכל דחיפה ל‑`main` |
| הסודות ב‑GitHub | `CLOUDFLARE_API_TOKEN` (מצומצם ל‑Pages בלבד), `CLOUDFLARE_ACCOUNT_ID` |
| אנליטיקס | ‏Cloudflare Web Analytics, ‏site `c36b9189…6d2f`, מוזרק על ידי Pages — **אין קוד בריפו** |

**‏`inplace-landing` הוא פרויקט שני ונפרד מ‑`supplyflow`**, שהוא פרויקט האפליקציה.
שתי הכתובות חולקות אזור DNS אחד ותו לא.

### שיטת הפריסה, ולמה

הפרויקט של האפליקציה (`supplyflow`) הוא העלאה ישירה, ולא בנייה מ‑GitHub. זו הצורה
שהחשבון הזה כבר עובד בה, וזו גם הצורה היחידה שניתן להקים בלי אישור OAuth בדפדפן של
אפליקציית Cloudflare בחשבון ה‑GitHub.

**דחיפה ל‑`main` כן מפרסמת — אבל לא Cloudflare עושה את זה.**
‏[.github/workflows/deploy.yml](.github/workflows/deploy.yml) בונה ומעלה בעצמו בכל דחיפה,
ואז **שואל את השרת החי** אם מה שפורסם עובד. פריסה שהעלתה קבצים ושברה משהו תהיה אדומה.

ידנית, כשצריך לפרוס בלי קומיט:

```bash
npm run deploy
```

הפקודה בונה מחדש ומעלה. היא דורשת `CLOUDFLARE_API_TOKEN` ו‑`CLOUDFLARE_ACCOUNT_ID`
בסביבה. אותו הדבר אפשרי גם מ‑GitHub, בהפעלה ידנית של `deploy`.

### שני מפתחות, ולמה שניים

**הריפו הזה ציבורי.** המפתח ששמור כסוד ב‑GitHub (`CLOUDFLARE_API_TOKEN`) יודע דבר אחד:
`Account → Cloudflare Pages → Edit`. נמדד לפני שנשמר — הוא רואה את פרויקטי ה‑Pages
ו**נדחה** על ה‑DNS של האזור. אם ידלוף אי‑פעם, הגרוע שיקרה הוא פרסום גרסה של דף הנחיתה.

המפתח הרחב, זה שחיבר את הדומיין ויצר את חוק ה‑`www`, יודע גם DNS וגם הפניות. **הוא לא
נשמר בשום מקום מקוון** ולא עוזב את המכונה של הבעלים. ההפרדה הזאת היא העניין: מפתח פרסום
אינו צריך לדעת מה זו כתובת.

---

## 2. מה נבדק לפני, על עותק נקי

הבנייה הורצה על `git clone --depth 1` של `origin/main` — לא על עותק העבודה — כדי
שמה שנמדד יהיה מה שיוצא לאוויר ולא מה שיושב על המכונה:

| # | מה נבדק | תוצאה |
|---|---|---|
| D1 | `npm ci` מהעותק הנקי | `added 91 packages`, ‏`0 vulnerabilities`, יצא `0` |
| D2 | `npm run build` מהעותק הנקי | יצא `0`. ‏`tsc --noEmit` נקי, ‏`vite build` ב‑6.46s, ‏18 עמודים |
| D3 | הבנייה אינה דורשת דפדפן | ‏`prerender.mjs` משתמש ב‑Vite בלבד |
| D4 | גודל קובץ בודד | הגדול ביותר `film-en.mp4` = **9.90 MiB**, התקרה **25 MiB** |
| D5 | מספר קבצים | **202**, התקרה **20,000** |
| D6 | זמן בנייה | שניות, התקרה **20 דקות** |
| D7 | חוקי `_headers` | **9**, התקרה **100** |
| D8 | הפלט מכיל את מה שצריך | `404.html`, `_headers`, `robots.txt`, `sitemap.xml`, מפתח IndexNow |
| D9 | הכתובות בפלט | ‏45 אזכורים של `inplace.digital`, ‏12 של `app.inplace.digital`, אפס זרות |
| D10 | חבילת השערים | **‏27 met, 0 unmet** על הקומיט שנפרס |

**תיקון אחד נדרש ובוצע לפני הפריסה:** נוסף `.node-version` עם `22.16.0`. תמונת הבנייה
הישנה של Cloudflare מגישה Node 18.17.1, ו‑Vite 7 דורש 20.19 ומעלה. הקובץ נשאר גם אחרי
המעבר להעלאה ישירה — הוא מקבע את הגרסה לכל מי שיבנה את הפרויקט הזה.

---

## 3. מה נעשה בפועל, לפי הסדר

| # | פעולה | תוצאה שנמדדה |
|---|---|---|
| 1 | יצירת פרויקט `inplace-landing` | `subdomain=inplace-landing.pages.dev`, `production_branch=main` |
| 2 | העלאה ראשונה מהעותק הנקי | **201 קבצים ב‑8.04 שניות**, `_headers` נקלט בנפרד |
| 3 | אימות מלא על `inplace-landing.pages.dev` | **7 met, 0 unmet** — לפני שהדומיין נגע בכלל |
| 4 | חיבור `inplace.digital` כ‑custom domain | `status=active`, `cert=active` (Google CA) |
| 5 | יצירת רשומת ה‑DNS ב‑apex | Cloudflare **לא** יצר אותה לבד, בדיוק כמו במעבר של `app.` — נוצרה ידנית |
| 6 | אימות מלא על `inplace.digital` | **7 met, 0 unmet** |
| 7 | חוק ה‑Single Redirect ל‑`www` | ‏ruleset `7b5327d768f5…a8f0`, פעיל |
| 8 | החזרת רשומת ה‑`www`, **אחרי** שהחוק כבר פעל | `301` לכתובת הראשית, עם הנתיב וה‑query |
| 9 | אימות מלא עם `www` | **8 met, 0 unmet** |
| 10 | מפתח פרסום מצומצם, ואימות שהוא באמת מצומצם | רואה Pages, **נדחה** על DNS — ורק אז נשמר ב‑GitHub |
| 11 | ‏`deploy.yml` ו‑`indexnow.yml` | הפעלה ראשונה: בנייה, העלאה של 6 קבצים שהשתנו (195 כבר היו), אימות |
| 12 | השרשרת עצמה, בכשל | האימות נכשל על `L8`, ‏`deploy` אדום, ו‑`IndexNow` **דילג על עצמו** |
| 13 | כיבוי ניהול ה‑`robots.txt` של Cloudflare (הבעלים) | הקובץ החי חזר להיות `public/robots.txt` מילה במילה |
| 14 | השרשרת עצמה, בהצלחה | `deploy` ירוק, **9 met 0 unmet**, ו‑`IndexNow` רץ אחריו: `18 URLs submitted, HTTP 200` |
| 15 | חיבור האנליטיקס | הרשומה כבר נוצרה ב‑23.08 עם הדומיין, אך לא הייתה קשורה לשום מארח ולא אספה דבר |
| 16 | הצמדתה לפרויקט ה‑Pages ופריסה | ה‑beacon מוגש בכל 18 העמודים, עם ה‑token הנכון, ו**אפס עוגיות** |

הסדר אינו שרירותי: האימות המלא רץ על כתובת ה‑`pages.dev` **לפני** שהדומיין הציבורי
הופנה לשם. אם משהו היה נשבר, הוא היה נשבר בכתובת שאיש לא מכיר.

**‏`www` נסגר באותה ישיבה, בשני סיבובים.** הראשון נדחה בהרשאה ובוטל בתוך דקות;
השני עבר אחרי שהבעלים הוסיף למפתח את `Zone → Single Redirect → Edit`. גם שם הסדר
אינו שרירותי: החוק נוצר **לפני** שרשומת ה‑`www` הוחזרה, כדי שלא ייפתח שוב חלון שבו
הכתובת עונה בלי הפניה. הסיפור המלא, כולל שלוש הדרכים שנבדקו ונדחו,
ב‑[DEBT.md](DEBT.md) §8.

---

## 4. מה נמדד על השרת החי

‏`node scripts/verify-live.mjs` הוא הפנקס הזה כקוד. הוא שואל **שרת**, לא בנייה, ולכן
הוא היחיד שיכול לענות על חמשת הדברים ש‑DEBT.md §5–§9 המתינו להם:

```bash
npm run verify:live                       # מול inplace.digital
node scripts/verify-live.mjs inplace-landing.pages.dev
```

| # | מה | נמדד 31.08.2026 |
|---|---|---|
| L1 | כל 18 העמודים עונים `200` | **PASS** |
| L2 | כתובת שגויה מחזירה `404` | **PASS** — לא `200` עם דף הבית |
| L2b | דף ה‑404 נושא `noindex` | **PASS** |
| L3 | חמש כותרות האבטחה חוזרות | **PASS** — Cloudflare קרא את `public/_headers` |
| L4 | `robots.txt` מוגש כ‑`text/plain` | **PASS** |
| L5 | `www` מפנה ל‑apex ב‑301 | **PASS** — עם שמירת הנתיב וה‑query |
| L6 | `http` מפנה ל‑`https` | **PASS** |
| L7 | ה‑sitemap מונה 18 עמודים על הדומיין הנכון | **PASS** |
| L8 | ה‑`robots.txt` ה**מוגש** אינו חוסם דבר | **PASS** — היה `9`, עכשיו `0` |
| L9 | ה‑beacon של האנליטיקס מוגש, עם token | **PASS** |

**‏10 met, 0 unmet**, גם מכאן וגם מתוך ה‑CI. שתיים מהן דרשו פעולה של הבעלים בלוח הבקרה:
‏`L5` את ההרשאה `Zone → Single Redirect → Edit`, ו‑`L8` את כיבוי ניהול ה‑`robots.txt`.

**‏`L9` קיימת מאותה סיבה ש‑`L8` קיימת.** אין בריפו שום זכר לאנליטיקס — לא סקריפט, לא
תלות, לא שלב בנייה. ‏Cloudflare מזריקה את ה‑beacon בהגשה, מהגדרה על הפרויקט, והגדרה היא
בדיוק מה שמישהו מכבה בלי לקרוא את הקובץ הזה. שום שער מקומי לא יכול להיכשל על זה.

**מה שכן השתנה בפועל, ונאמר כאן במפורש:** שישה עמודים בדף הזה נבנו בלי שורת JavaScript
אחת, והם מגישים עכשיו את ה‑beacon. השער `g20-static` עדיין עובר — הוא בודק היעדר
`type="module"`, וה‑beacon אינו כזה — אבל „בלי JavaScript” פירושו מהיום **בלי JavaScript
של האפליקציה**, ולא בלי שום סקריפט. ‏Cloudflare Web Analytics אינו מניח עוגיות, ולכן זה
לא גורר באנר הסכמה ולא שינוי במסמכים המשפטיים — נמדד: `set-cookie` אינו חוזר כלל.

`L2` הוא הקריטי, והוא זה שלא ניתן היה למדוד קודם. **אסור להוסיף קובץ `_redirects` עם
‏`/* /index.html 200`.** הכלל הזה הופך כל כתובת שגויה ל‑`200` עם דף הבית, כלומר מספר
בלתי‑מוגבל של עמודים חוקיים בעיני גוגל. הריפו הזה אינו SPA: כל 18 העמודים קיימים
כקבצים, ו‑Cloudflare מגיש את `404.html` לבד. **אין מה להוסיף, ויש מה לא להוסיף.**

---

## 5. מה נשאר פתוח

### 5.1 מספרי המסלולים

‏[DEBT.md](DEBT.md) §1: שתים‑עשרה קבוצות מספרים בדף אין להן היום מקור חי באפליקציה,
והכיוון לטובת הלקוח — הדף מבטיח פחות ממה שהמוצר נותן. **הבעלים החליט 31.08.2026 לעלות
ככה.** נסגר בפריסת המיגרציות `0243`–`0267` באפליקציה, לא בשינוי כאן.

---

## 6. גלגול אחורה

| מה | איך |
|---|---|
| להוריד את הדף מהאוויר | מחיקת רשומת ה‑`CNAME` ב‑apex. הכתובת חוזרת להיות ריקה — מצבה עד 31.08.2026 |
| לנתק את הדומיין לגמרי | בנוסף, ניתוק ה‑custom domain מהפרויקט |
| לחזור לפריסה קודמת | כל פריסה ב‑Pages נשמרת ככתובת בפני עצמה; החזרה היא Rollback בלוח הבקרה |
| לבטל את הפניית ה‑`www` | מחיקת החוק **ואחריה** מחיקת רשומת ה‑`www`. בסדר הזה: רשומה בלי חוק מחזירה `523` |
| לכבות אנליטיקס | ריקון `web_analytics_tag` ו‑`web_analytics_token` בפרויקט ה‑Pages, ואז פריסה. `L9` תיכשל, וזו הכוונה |

**האפליקציה אינה מושפעת מאף אחד מהם.** ‏`app.inplace.digital` יושב על פרויקט אחר
ועל רשומת DNS אחרת.

---

## 7. מה במפורש לא נעשה

- **אין `_redirects`.** ראה §4.
- **אין CSP.** הוחלט ב‑`public/_headers`: מגיע כ‑Report-Only קודם.
- **אין `preload` ב‑HSTS.** כמעט בלתי הפיך, וחל גם על `app.inplace.digital` — החלטה
  של הבעלים ולא תופעת לוואי של פריסת דף נחיתה.
- **רשומות ה‑MX, ה‑SPF, ה‑DKIM וה‑DMARC של השורש לא נגעו.** הן מישור הדואר.
- **‏`supplyflow`, ‏Supabase והאפליקציה לא נגעו.** הפריסה הוסיפה פרויקט אחד ורשומת
  DNS אחת.
